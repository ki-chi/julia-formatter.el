(require 'ert)
(require 'julia-formatter)

(eval-when-require (require 'json))

(ert-deftest jf-test-format ()
  "Test of `julia-format`, the core of the packages' functions"
  )

(ert-deftest jf-test-format-region ()
  "Test of `julia-format-region`"
  )

(ert-deftest jf-test-format-buffer ()
  "Test of `julia-format-buffer`"
  )
